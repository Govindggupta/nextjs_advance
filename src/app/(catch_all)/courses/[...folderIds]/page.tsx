export default function(params: any ) {

    return (
        <div>
            folderIds : {JSON.stringify(params)}  
        </div>
    )
    // this will all the route we write after courses , but what about /courses ? 
}